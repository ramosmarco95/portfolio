version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app               # live code
      - /app/node_modules    # keep container's node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

FROM node:20-alpine
WORKDIR /app

# Install deps first for better caching
COPY package*.json ./
RUN npm ci || npm install

# Copy source
COPY . .

# Vite dev server
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
